<div class='col-md-12 text-center'>
	<% if current_user %>
	  		<div class="text-center">
	  			<h1 class="main-text">Welcome, <%= current_user.name %></h1>
	  			<!-- <h4>Logged In with -> <strong><em><%= @auth['provider'].capitalize %></em></strong></h4> -->
	  		</div>

			<%= link_to 'Start Game', game_path, id: "link1", class: 'btn btn-primary text-center'%>
			<% else %> 
			<div class="erven"><%= image_tag("welcomegame.jpg", :alt => "rss feed") %></div>

	<% end %>
</div>
